export default class Tenant {
  constructor(
    ID,
    firstname,
    lastname,
    adress,
  ) {
    this.ID = ID;
    this.firstname = firstname;
    this.lastname = lastname;
    this.adress = adress;
  }
}